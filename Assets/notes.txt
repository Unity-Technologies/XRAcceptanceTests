Performance Test:
-Haze Material: Write in ShaderGraph, remove depth read/write.

Terrain:
-Add tree/detail objects once support is added.

Effects:
-Removed projectors because they are unsupported with LWRP.

Materials:
-Removed Stencil (unsupported)
-Removed Set Target Buffer (unsupported)

=====================================================
2019.1

Disabled Hololens Editor Scripts:
-Not supported with 2019.1 due to API changes.
-Add back later after support is restored.

HaloShader could not be updated due to removal of shaderGraph->master node->copy shader.  This was required for passing depth tests for an overlay effect.